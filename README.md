# Elastic Doc Serivce (PoC)

## Creating this project

```bash
mvn archetype:generate -DgroupId=hk.com.asl.egms2 -DartifactId=elastic-doc-service -DarchetypeArtifactId=maven-archetype-quickstart -DinteractiveMode=false
```

## Run the project

```bash
mvn spring-boot:run
```